name: Dotnet Pack
description: Restore, build and pack .NET project

inputs:
  project_folder:
    description: Path to the .NET project to pack
    required: true
  output_folder:
    description: Path for generated NuGet packages
    required: true

runs:
  using: composite
  steps:
    - uses: actions/setup-dotnet@v6
      with:
        global-json-file: ./global.json

    - name: Restore
      run: dotnet restore ${{ inputs.project_folder }}
      shell: bash

    - name: Pack
      run: dotnet pack ${{ inputs.project_folder }} --configuration Release --output ${{ inputs.output_folder }} --no-restore
      shell: bash
